# HITU AI Engine

This directory is reserved for future dedicated AI workers and orchestration services.

Current scheduling intelligence lives in `apps/backend/app/services/scheduler.py` using OR-Tools CP-SAT, while Ollama is exposed to the backend through `OLLAMA_URL` and `OLLAMA_MODEL`.
